<%= render 'rating' %>
<%= render partial: 'review_responses', locals: { admin: 'Você' } %>

<%= form_with(model: [@rating, @review_response]) do |f| %>
  <%= f.label :comment, "Responder #{ @rating.user.name}" %>
  <%= f.text_field :comment %>

  <%= f.submit 'Responder' %>
<% end %>
